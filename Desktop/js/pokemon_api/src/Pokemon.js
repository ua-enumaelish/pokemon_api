import React from "react";

export default function Pokemon({props}) {
  console.log(props);
  return(<>
    <div className="container">
      <header>
        HELLO
      </header>
    </div>    
  </>)
}